{
  "name": "mashlib-next",
  "version": "0.0.5",
  "type": "module",
  "description": "Solid data browser — drop-in replacement for mashlib",
  "license": "AGPL-3.0-or-later",
  "keywords": ["solid", "mashlib", "linked-data", "rdf", "data-browser", "solidOS"],
  "homepage": "https://mashlib.com",
  "main": "mashlib.js",
  "files": [
    "mashlib.js",
    "mashlib.css",
    "shim-*.js",
    "jsonld-*.js",
    "marked.esm-*.js"
  ],
  "scripts": {
    "dev": "pnpm --filter @mashlib-next/shell dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist .tsbuildinfo"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "typescript": "~5.8.3",
    "vite": "^7.3.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
